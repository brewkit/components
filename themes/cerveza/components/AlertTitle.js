import { size } from '../variables';
var AlertTitle = {
    overrides: {
        MuiAlertTitle: {
            root: {
                fontSize: size.normal,
                marginBottom: 0
            }
        }
    }
};
export default AlertTitle;
