import { color } from '../variables';

const Breadcrumbs = {
    overrides: {
        MuiBreadcrumbs: {
            separator: {
                color: color.blueGray.dark,
            },
        },
    },
};

export default Breadcrumbs;
