import { ReactElement } from 'react';
import { Props } from './types';
declare function PlaceholderAction({ className, variant, onClick, children, }: Props): ReactElement;
export default PlaceholderAction;
