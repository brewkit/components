import React from 'react';
import { Props } from './types';
declare const InputBase: ({ after, as, before, checked, children, className, cols, customControl, defaultChecked, defaultValue, disabled, error, fullWidth, id, inputClassName, inputLabel, inputLabelClassName, name, onChange, placeholder, readOnly, required, rows, type, value, ...otherProps }: Props) => React.ReactElement<any, string | ((props: any) => React.ReactElement<any, string | any | (new (props: any) => React.Component<any, any, any>)>) | (new (props: any) => React.Component<any, any, any>)>;
export default InputBase;
