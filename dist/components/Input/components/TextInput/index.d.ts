import { ReactElement } from 'react';
import { Props } from './types';
declare function TextInput({ after, before, className, error, disabled, type, ...otherProps }: Props): ReactElement;
export default TextInput;
