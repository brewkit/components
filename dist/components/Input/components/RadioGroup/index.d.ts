import { ReactElement } from 'react';
import { Props } from './types';
declare function RadioGroup({ name, value, defaultValue, onChange, className, children, ...otherProps }: Props): ReactElement;
export default RadioGroup;
