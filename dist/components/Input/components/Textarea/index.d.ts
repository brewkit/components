import { ReactElement } from 'react';
import { Props } from './types';
declare function Textarea({ after, before, className, disabled, error, rows, ...otherProps }: Props): ReactElement;
export default Textarea;
