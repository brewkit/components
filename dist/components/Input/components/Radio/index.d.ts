import { ReactElement } from 'react';
import { Props } from './types';
declare function Radio({ className, disabled, fullWidth, inputLabelClassName, ...otherProps }: Props): ReactElement;
export default Radio;
