import { ReactElement } from 'react';
import { Props } from './types';
declare function Dialog({ cancelText, children, className, closeIcon, color, confirmText, customFooter, hasIcon, hideFooter, icon, isOpen, onBackgroundClick, onConfirm, onCancel, title, variant, ...otherProps }: Props): ReactElement;
export default Dialog;
