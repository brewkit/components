import { ReactElement } from 'react';
import { Props } from './types';
declare function FormField({ className, inputClassName, error, helperText, label, variant, ...otherProps }: Props): ReactElement;
export default FormField;
