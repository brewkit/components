import { ReactNode } from 'react';

export interface Props {
    /**
     * Used to add a custom className to the component
     */
    className?: string,
    /**
     * The children of the component
     */
    children?: ReactNode,
}
