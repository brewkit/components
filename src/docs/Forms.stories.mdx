import { Meta, Source } from '@storybook/addon-docs/blocks';


<Meta title="Documentation|Forms" />


# Forms

Forms are generally comprised of three components:

1. `<Form />` - Wraps the form's fields and determines what to do when the form is submitted.
2. `<Form.Field />` - Renders an input field and binds it to the `Form`. Also handled input validation.
3. `<Form.Submit />` - The submit button usually at the end of a form. Will lock and unlock based on validity of the inputs.

For better usage examples, check out the [documentation on Form](/?path=/docs/stable-forms-form--general).

<br />

## Validation

The preferred way of validating a `Form` is using [yup](https://github.com/jquense/yup) validation schema. The validation schema
is handed off to `Form` using the `validationSchema` prop.

<Source code={(`
    const validationSchema = yup.object({
        myField: yup.string().required('This field is required.').min(10, 'A length of 10 or more is required.'),
    });\n
<Form validationSchema={validationSchema}>
        <Form.Field label="Required TextField" name="myField"/>
</Form>
`)} />
